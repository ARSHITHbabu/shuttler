# FastAPI and Server
# A16: Updated from 0.104.1 — fixes PYSEC-2024-38 (ReDoS via python-multipart form handling)
fastapi==0.115.4
# A16: Updated from 0.24.0
uvicorn[standard]==0.32.1

# Database
sqlalchemy==2.0.23
psycopg2-binary==2.9.9  # PostgreSQL adapter
alembic==1.13.0  # Database migrations

# Pydantic for data validation
pydantic==2.5.0
pydantic-settings==2.1.0

# Authentication & Security
# A16: python-jose 3.3.0 has known CVEs (PYSEC-2024-232, PYSEC-2024-233 — algorithm confusion).
# No fixed release is currently available upstream (project is largely unmaintained).
# Our JWT config uses HS256 (HMAC) only — NOT ECDSA — which mitigates the ECDSA-specific issues.
# Migration to joserfc or PyJWT is recommended before public production launch.
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
slowapi==0.1.9

# File handling
# A16: Updated from 0.0.6 — fixes PYSEC-2024-38, CVE-2024-53981, CVE-2026-24486 (ReDoS / DoS)
python-multipart==0.0.22

# Environment variables
python-dotenv==1.0.0

# Firebase for push notifications (B5)
firebase-admin==6.3.0

# Transactional email (B11)
sendgrid==6.11.0

# Testing
pytest==7.4.3
httpx==0.25.2
pytest-asyncio==0.21.1
pytest-cov==4.1.0
pytest-mock==3.12.0

# Schema registry auto-discovery
watchdog==3.0.0  # File watcher for automatic schema registry updates (optional but recommended)

# Background Tasks
apscheduler==3.10.4

# Payment Gateway (B12)
razorpay==1.4.1

# S3 File Storage (C7)
boto3==1.34.0

# Redis Cache (C8)
redis==5.0.1
fastapi-cache2==0.2.1
